/**
 * No-op animation placeholders to avoid requiring @angular/animations dependency.
 * Components can still reference these in the animations array without errors.
 */
export const fadeInOut: never[] = [];
export const scaleIn: never[] = [];
